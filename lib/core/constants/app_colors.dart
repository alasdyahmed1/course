import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryLight = Color(0xFFFCFDFF);
  static const Color primaryMedium = Color(0xFFE6F2FF);
  static const Color primaryBg = Color(0xFFD0E6FF);

  // Additional colors
  static const Color accent = Color(0xFF007AFF);
  static const Color success = Color(0xFF28A745);
  static const Color error = Color(0xFFDC3545);
  // static const Color warning = Color(0xFF7691C1); //   لون أزرق فاتح
  static const Color warning = Color(0xFF6B8CC7); // درجة أغمق قليلاً
  static const Color info = Color(0xFF007AFF); // لون أزرق للمعلومات

  // Text colors
  static const Color textPrimary = Color(0xFF25467F);
  static const Color textSecondary = Color(0xFF7691C1);

  // Action colors
  static const Color buttonPrimary = Color(0xFF25467F); // Dark blue for login
  static const Color buttonSecondary =
      Color(0xFF0077FF); // Bright blue for actions
  static const Color buttonThird =
      Color.fromARGB(187, 0, 119, 255); // Bright blue2 for actions
  static const Color hintColor = Color(0xFF7691C1); // Hint/subtitle color
}
